# Default Templates

Place the CTE lesson plan template here as `cte-lesson-plan.docx`.

This template should be a .docx file with the standard CTE lesson plan format:
- 18-row table structure
- Checkboxes for daily activities
- Merged cells for headers
- Standard formatting

The template will be used as the default option in the template selector.
